# Postgres connection used by app/database.py (local/dev target — see research.md Decision 2)
DATABASE_URL=postgresql+psycopg2://po_app:po_app@localhost:5432/po_workflow

# JWT signing secret and access-token lifetime (app/auth.py)
JWT_SECRET=dev-secret-change-me
JWT_EXPIRE_MINUTES=60
